# Site Build Settings

# jekyll-paginate configuration
paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag, jekyll-toc]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title: WorldWideWebster
description: Does anyone actually read this?
author: Sean Webster

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
  
# Auto table of contents
kramdown:
  parse_block_html: true
  
# Google Analytics
google_analytics: G-Z1BV878J0D